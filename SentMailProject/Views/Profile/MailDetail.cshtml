@model SentMailProject.Models.DTO.SentMailDTO
@{
    Layout = "~/Views/Shared/_ProfileLayout.cshtml";
}


<link href='~/lib/bootstrap/dist/css/bootstrap.css' rel='stylesheet' type='text/css'>

@*<nav class="navbar justify-content-between">
    <a></a>
    <form class="form-inline">
        <a class="navbar-brand my-2">@Model.appUser.Name @Model.appUser.Surname</a>
        <a href="/Login/Index/" class="btn btn-outline-danger my-2 my-sm-0">Çıkış Yap</a>

    </form>
</nav>*@
<div class="row">
    <div class="mx-auto">
        <div class="card" style="width: 38rem;">
            <div class="card-body" style="min-height:20rem;">
                <div class="mt-4">
                    <h6 class="card-subtitle mb-2 text-muted">Gönderici: @Model.sender.Name @Model.sender.Surname (@Model.sender.Email)</h6>
                    <h6 class="card-subtitle mb-2 text-muted">Alıcı: @Model.toWho.Name @Model.toWho.Surname (@Model.toWho.Email)</h6>
                </div>
                <div class="mt-4">
                    <div class="mt-4">
                        <h6 class="card-subtitle text-muted float-right">@Model.sentMail.Date</h6>
                        <h5 class="card-title">@Model.sentMail.Subject</h5>

                    </div>

                    <p class="card-text">@Model.sentMail.Message</p>
                </div>
                <div class="mt-5">
                    <a href="#" class="btn btn-outline-info">Cevapla</a>
                    <a href="#" class="btn btn-outline-danger">Sil</a>
                </div>


            </div>
        </div>
    </div>
</div>